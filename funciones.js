function comparar(){
 const a = Number(prompt("Ingresa un número:"));
 const b = Number(prompt ("Ingresa otro número:"));

 if (a > b){
    alert (a + " es mayor que " + b);
 } else {
    alert (b + " es mayor que " + a);
 }
}
